<h1>Tickets</h1>

{% for ticket in tickets %}

    <div> {{ ticket.id }}</div>
    <div> {{ ticket.title }}</div>
    <div> {{ ticket.description }}</div>
    <div> {{ ticket.createdat.format('d-M-Y h:i:s') }}</div><br>

{% endfor %}